# Interpretability alignment - findings

Generated by experiments/interpretability_alignment.py (tag=egfr, split=scaffold, top_bits=5, top_atom_frac=0.25).

## Numbers
- molecules analysed: 10 (chromone x5, thienopyrimidine x5); both methods ok on 10
- GNN top atoms inside RF top-bit coverage: mean 0.47 (min 0.40, max 0.71)
- GNN top atoms sitting on the scaffold core: mean 0.66

## Findings
1. **chromone**: cross-model alignment mean 0.40 (n=5). Highest: mol1 (overlap 0.40, `Nc1ccc(-c2cc(=O)c3cc(N)c(O)cc3o2)cc1`); lowest: mol1 (overlap 0.40, `Nc1ccc(-c2cc(=O)c3cc(N)c(O)cc3o2)cc1`).
1. **thienopyrimidine**: cross-model alignment mean 0.54 (n=5). Highest: mol2 (overlap 0.71, `OCc1cccc(-c2cc3c(N[C@H](CO)c4ccccc4)ncnc3s2)c1`); lowest: mol4 (overlap 0.43, `C[C@@H](Nc1ncnc2sc(-c3cccc(S(C)(=O)=O)c3)cc12)c1cc`).
2. **chromone RF recurring bits**: bit 1367 (top-5 in 5/5 mols), bit 1645 (top-5 in 5/5 mols), bit 1452 (top-5 in 5/5 mols) - recurring bits indicate the substructures driving the RF on this chemotype.
2. **thienopyrimidine RF recurring bits**: bit 1367 (top-5 in 5/5 mols), bit 329 (top-5 in 5/5 mols), bit 1645 (top-5 in 5/5 mols) - recurring bits indicate the substructures driving the RF on this chemotype.
3. **GNN attention on scaffold core**: mean fraction of GNN top atoms on the family scaffold core = 0.66 (values mol1:0.80, mol2:0.80, mol3:0.80, mol4:0.80, mol5:0.80, mol1:0.50, mol2:0.43, mol3:0.50, mol4:0.43, mol5:0.71).

## Caveats
- GNNExplainer attributions are post-hoc and approximate; read the
  heatmaps as indicative, not causal.
- SHAP bit values are exact for the retrained RF (deterministic);
  the RF was retrained on the 4438 paired pool (no committed
  checkpoint existed for this pool).
- Cross-model atom alignment is judged by set overlap of two
  different attribution vocabularies; treat low overlap as
  'attends differently', not as either model being wrong.
